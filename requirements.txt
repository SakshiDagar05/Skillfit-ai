# SkillFit AI — Python Dependencies
# Install all with: pip install -r requirements.txt
# Recommended: Python 3.11

# ── Web Framework ──────────────────────────────────────────
fastapi==0.111.0
uvicorn==0.29.0
python-multipart==0.0.9
pydantic==2.7.1

# ── Speech Recognition (Whisper ASR) ───────────────────────
openai-whisper==20231117
ffmpeg-python==0.2.0

# ── NLP & Transformers ─────────────────────────────────────
transformers==4.41.0
datasets==2.19.1
tokenizers==0.19.1
sentencepiece==0.2.0
sacremoses==0.1.1

# ── PyTorch (CPU version — works on all laptops) ───────────
# For GPU support replace with: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
torch==2.3.0
torchvision==0.18.0
torchaudio==2.3.0

# ── Computer Vision ────────────────────────────────────────
mediapipe==0.10.14
opencv-python==4.10.0.82
Pillow==10.3.0

# ── Audio Analysis ─────────────────────────────────────────
librosa==0.10.2
soundfile==0.12.1
audioread==3.0.1
pydub==0.25.1

# ── Data Science & ML ──────────────────────────────────────
scikit-learn==1.4.2
pandas==2.2.2
numpy==1.26.4
scipy==1.13.0

# ── Duplicate Detection ────────────────────────────────────
faiss-cpu==1.8.0

# ── Utilities ──────────────────────────────────────────────
python-dotenv==1.0.1
requests==2.31.0
tqdm==4.66.4
httpx==0.27.0
aiofiles==23.2.1

# ── HuggingFace Hub (model downloads) ─────────────────────
huggingface-hub==0.23.1
accelerate==0.30.1

# ── CORS & API ─────────────────────────────────────────────
starlette==0.37.2

# ── Optional: for Aadhaar / ID verification ────────────────
# pytesseract==0.3.10
# pdf2image==1.17.0
